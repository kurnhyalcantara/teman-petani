# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=myuser
DB_PASSWORD=mypassword
DB_NAME=mydatabase
DB_SSL_MODE=disable        # Example: disable, require, verify-ca, verify-full
DB_TIMEZONE=UTC            # Timezone for the database connection
DB_RETRY=3                 # Number of retries for database connections
DB_TIMEOUT=30              # Timeout in seconds for database connections
